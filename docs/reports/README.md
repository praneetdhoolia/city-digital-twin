# Project reports

Dated, self-contained assessments of the whole repository, produced by the
`/project-report` skill ([`.claude/skills/project-report/SKILL.md`](../../.claude/skills/project-report/SKILL.md)).
Each is a reading of one commit: every file by area, code quality with
file-and-line evidence, the milestones of every pull request and commit, the
issue ledger, CI, runs, data and documents. Newest first; a report is never
overwritten.

[`reference/`](reference/README.md) is the **standing reference library** — the
survey of comparable city twins and the literature on what a decision simulator
must contain, each row carrying its sources and the date it was last verified.
The research phases read it before searching and spend their budget only on
what it does not answer. A dated report is prunable; the library is not, and it
is what survives one being deleted.

A row below with no link is a report whose file has been pruned from the tree.
The row stays: it is the dated record of what was assessed at which commit, and
the reference library cites it as the source of its seeded rows. The file itself
is recoverable from git history.

| Report | Commit read | Date | Headline |
|---|---|---|---|
| [`20260910T134723_project_report.html`](20260910T134723_project_report.html) | `4d1d1bc` (the PR #173 merge, on `main`, clean tree) | 10 September 2026 | **The instruments were not attached to the thing they measured — 10 defects, 19 risks and 15 smells at HEAD, and the answer to why running to convergence made the fit worse**: the same defect appears **five times in five review lanes** — an instrument or a lever not connected to what it was believed to measure or control — and the sharpest is that **the road-counts validation rung has been measuring nothing for 25 days**: `count_station_links.csv` was written 2026-08-16T00:37:54 and the S2 network rewritten **47 minutes later**, so of 195 mapped rows **none names the road it claims** (64 point at service roads, 61 at residential streets, 8 at links that no longer exist), station 55870 "Maitland Road" scores *Talleyrand Circuit*, a residential cul-de-sac at zero, while the real Maitland Road carries a median **18,506** veh/day against an observed **19,373 (−4.5 %)** — so the reported **−89.35 % mean / −98.7 % median / 7 zeros is spurious**, issue #82 and the through-traffic tier built to answer it addressed a phantom, and the ×1/fraction fold and network volume are both *correct* (car veh-km agree with `pkm_modestats` to **1.19 %**); the equilibrium question is answered from the landed arm's own thirteen read points — **the observed city is not a fixed point of this model but a TRANSIENT the search passes through around iteration 50–100**, car and walk cross their targets near it.60 and keep going, **taxi (+88.5 → +202.2 %) and light rail (−7.9 → −57.3 %) are closest to observation AT THE SEED and driven away by the dynamics**, ride never moves off −38 %, and the best the project has ever been at any read point of any run is **2 of 12 inside 10 %**; the mechanism is that **car's share is nearly independent of distance** (63.1 % of trips *under one kilometre*, 76.9 % over twenty) because `marginalUtilityOfDistance` is **0.0 for every mode**, car's constant is 0.0 against walk's +0.35, `routing.accessEgressType` is **`none`** (one field behind both this and the #167 pt-teleportation breach) and parking is charged only in ~150 of 1,500 zones between 08:00 and 18:00 — so **the model charges no fixed per-trip cost for using a car**, and car's share of sub-kilometre trips rises monotonically **45.4 → 63.1 %** as the search converges; **two defects sit at two layers and only one is the equilibrium's to fix** — the *supply* of short trips moves only 12.46 → 11.17 % across 300 iterations because destination placement fixes it at build time (#30), so walk's −26.5 % has a ceiling no behavioural parameter can lift; a one-field candidate cause is that **`fractionOfIterationsToStartScoreMSA` is `null` and undeclared** while the MATSim book pairs it with the innovation cutoff, and it predicts **both** measured symptoms (the +2.211 pp car snap and the average plan score peaking 19.2049 then falling to 14.5679); history is blunt — **60 arm launches, 6 ever reached their declared horizon, and between 21 Aug and 9 Sep not one of 54** — with the inside-10 % count running 0,1,0,1,0,0,1,1,0 and only bus, motorbike and car ever inside, none held; top optimisation costs **nothing**: both stop-watchers are built and were **both off** on the landed arm while **37.4 % of all 405.3 machine hours bought no citable reading**; **19 document contradictions**, four self-contradictions in one file, twelve of thirteen position pages still denying the project has a result, and **21 of 40 rules unenforced** including "never commit to `main`"; still the only agent-based project of **50** on the per-mode rung and it remains empty (7 / 10 / 25 / 8 / 0), now qualified — a four-step model *does* report it — with the field's own verdict that *"not all calibration and validation targets can be achieved simultaneously"* and Chen et al. (2020) naming existing methods **"myopic heuristics"** against exactly this target set, while the architecture that fits is a **maximum implausibility, not a weighted sum** — and this project's objective already **is** that maximum over its twelve modes (`CAL.objective.components` = `goal_modes.max_abs_rel_pct`) — a first draft of this report said otherwise by reading the **frozen 24 August `C5_calibration.json` record as live state**, and the report carries the correction as the sixth instance of its own pattern, the only one that caught the report itself; what remains is a **denominator**, since the objective has no model-discrepancy or replication-band term and a MATSim run is not bit-reproducible; factor ledger **IN 46 · PARTIAL 32 · INERT 2 · ASC 1 · OUT 9** over 90 rows, top mover **choice-set coverage**, with pt reaching 25.78 % of agents and coverage **frozen** after the cutoff |
| [`20260909T003402_project_report.html`](20260909T003402_project_report.html) | `781cf41` (the PR #168 merge, on `main`, clean tree) | 9 September 2026 | **Twelve targets, three decision layers, and one of them has no controls — 25 defects, 31 risks and 19 smells at HEAD, and the finding that redirects the whole calibration effort**: the previous report asked which search method could fit twelve targets and **twelve of its twenty recommendations landed overnight** (the objective restated onto the goal's twelve modes, the movable set 5 → 21, crowding reaching scoring for the first time, the ASC contraction implemented, 336.4 GiB reclaimed, lineage 512 → 0 undetermined) — so this pass could ask the next question, and the answer is that **the search method was never the binding constraint**. `RUN.mode_choice.modes` offers `pt` as ONE alternative, the submode is chosen downstream by a raptor whose cost §9.158 measured to carry **no mode constant, no fare and no distance term**, and a whole-file pass over the F31 arm's plan memory (**154,347 persons**) finds only **974 — 0.63 % of the population** — holding plans that differ in which pt submode they use, so a scoring constant can reallocate between bus, rail, tram and ferry for **under one per cent of agents**; therefore the ~15 h ASC contraction test, built and unrun, is **decisive for bike and undecidable as designed for bus, light rail and ferry**, because a null result would be equally consistent with taste, with mechanism, and with the control not being connected to the choice. The same class appears three times at three distances from the decision — `beta_headway`/`beta_reliability` reach a params JSON and never MATSim (INERT, and now the **top scoreboard mover**), the pt ASCs reach scoring but not the router, crowding reached nothing until yesterday — and **`check_hardcoding.py` catches a value typed into a script while nothing catches a value declared in the registry that arrives in the wrong layer**. Requirement 8 gets a **derived answer for the first time**: Aitken extrapolation over the F31 arm's own trajectory puts settling at **iteration 200–210**, the run is **not relaxed at 100** (average score 24 % below its asymptote, still rising), the choice set closes at **iteration 74** with pt reaching only 24.58 % of agents, and three falsifiable predictions follow — **`ride` is already converged at −38.0 % and no constant will ever move it**, **`car` (the only mode inside the band) is drifting further out**, and **`walk` gets worse**; top optimisation is still zero-cost — correct the depth arm's overlay, whose description claims a `#167` repair its `set` block does not apply and which §9.159 ruled must not run, then spend **0.5 h probing HEAD bytecode before the 32 h approval**, because `arm_cost.py` filters on fraction and profiled only and prices a stack that no longer exists while the last two runs died at iteration 0; **two fixes did not land because each was verified against the half of the code it changed** — the calibrator now sends `--config-set` while `find_run` still matches the raw `--set` channel, so a candidate dies *after* paying a full arm, and the issue gate passes because its regex matches the sentence describing the gate, waving through the three issues that open "This issue BLOCKS the launcher"; **twelve contradictions, every one on a position page**, two of them *self*-contradictions in adjacent bullets, and all twelve outside `check_doc_currency.py`'s reach; still the only project of **49** aiming at the per-mode rung and **nobody is on it** (7 / 9 / 25 / 8 / 0), though a four-step model *does* report it, so the empty rung is partly a reporting convention; the field has **no architecture that solves "all N inside a band"** — history matching and epsilon-constraint are the only two built for it and neither is used in transport — while this project's own contraction test is sharper than anything surveyed; factor ledger **IN 45 · PARTIAL 27 · INERT 2 · ASC 1 · OUT 8** over 83 rows with **five carried factors changing class, the first movement the library has recorded**; and the whole critical path is four steps of which **only one costs machine time** |
| [`20260908T192638_project_report.html`](20260908T192638_project_report.html) | `6cf0ffd` (the PR #161 merge, on `main`, clean tree) | 8 September 2026 | **Twelve targets, five knobs, and no loop between them — 15 defects, 63 risks and 60 smells at HEAD, and the finding that reframes the project**: `calibrate.py --plan` prints, verbatim, *"refusing to fit 5 parameters to 4 independent numbers: that is not a calibration"* — the whole automated search reaches **5 registry fields of 482**, two of them taxi-only, the single mode constant among them belongs to **taxi, the one mode with no independent target**, and one (`A.gradient.grade_clamp_pct`) reaches nothing at run time at all, so **not one movable parameter can touch any of the seven modes past the stop bar**; the path has moreover **never executed a candidate** — `calibrate.py:486-491` hands registry keys to the raw MATSim `--set` and `build_config:433-436` splits on the first dot — while `C5_calibration.json` honestly records `free_parameters: []`, `search_declined` and `feasible: false`, generated **24 August**, fifteen days and twenty-five families ago; the objective it would optimise is a **mean over five folded categories in pp** against a goal that is a **maximum over twelve unfolded modes in relative %**, with heavy and light rail opposite-signed inside one folded cell, so the calibrator could improve while both got worse; **identifiability is NOT the blocker** — ten targets against ten constants is a square, well-posed inversion, but six constants are frozen by §8.5, two do not exist (motorbike hard 0.0, ferry inherits `asc_bus`) and one is free and pointed at the targetless mode; history says the scoreboard **oscillates 0↔1 with no run of two** across eight valid family gates, the deepest citable `reached_iteration` is **100** of a 250 goal across all 162 runs, and the project's two deepest runs (67.4 h and 67.3 h, 21 Aug) carry **no `completion` at all** because the record contract postdates them; one evaluation is **7.66 h measured** (3.1/day) and pace is **sub-linear** in sample fraction (exponent 0.630 — the full timetable runs at every fraction), so 25%→10% buys 2.25×, not 6×; top optimisation: trim the two pre-repair arms, **336.4 GiB** of a 93 %-full store, touches nothing; still the only project of **48** aiming at the per-mode rung and **nobody is on it** (7 / 9 / 25 / 7 / 0), and the survey's new `calibration_methods` column finds the field's answer is the **ASC log-ratio fixed point** — affordable here at 5–15 rounds — while ML's one unblocked route is a **surrogate over the simulator** (477 parameters fitted from aggregate shares alone in ~150 evaluations), learned *behaviour* models being cleanly blocked by aggregate-only survey data; factor ledger IN 39 · PARTIAL 29 · INERT 2 · ASC 1 · OUT 8 over 79 rows with **not one carried factor changing class**, its top mover still crowding against heavy rail at **+247.2 %**, which has no brake of any kind; the recommended first move costs **zero machine hours** (restate the objective) and the decisive experiment costs **~15 h** and **opens no family** |
| [`20260907T231739_project_report.html`](20260907T231739_project_report.html) | `f40d332` (the PR #156 merge, on `main`, clean tree) | 7 September 2026 | **21 defects, 64 risks and 46 smells at HEAD, and the instrument is now cheap enough to run while nothing has been allowed to run**: the iteration is decomposed and cut to 205.5 s with this project's own code down from 56.2 % of it to **0.4 %**, yet `arm_cost.py:131` excludes every profiled run and so the launcher quotes **26.3 h** for the next 250-iteration arm against a measured **13.5–18 h**, asking the operator to approve 8–12 hours the model no longer needs; requirement 8 is not failing but **untested** — across 158 runs and 28 days the deepest reading is iteration 100, and 38 % of all 374 machine hours bought no citable reading; the board is red on one stale run and contradicts itself four ways (`STATUS.md:79` calls F30 "built and unlaunched" nineteen lines above the paragraph saying its arm was stopped at 23, while `:50` reads from that arm); top optimisation: an operator stop never writes `_metrics.json`, so `results_store.py:377` keeps 72.8 GiB of dead arms forever, and two log-storm arms hold a further 336 GiB of a 90 %-full store (both touch nothing); the scoreboard went **1 → 0 modes inside 10 %** in six hours (car +6.6 % at the F28 gate, −13.0 % at F30's iteration 20 — different families, not comparable); still the only project of **46** surveyed aiming at the per-mode rung and **nobody is on it** (7 / 9 / 24 / 6 / 0); factor ledger IN 37 · PARTIAL 27 · INERT 2 · ASC 1 · OUT 5, its top mover crowding against heavy rail at **+417.1 %**, which has no brake at all, and its cheapest `TramPriorityController.java` — written, registered and switched off — against light rail at −30.0 %; the reference library paid for itself: **0 stored rows re-searched** across both lanes, 56 calls spent only on its own gaps, both lanes stopping on the two-empty-rounds rule |
| [`20260907T173435_project_report.html`](20260907T173435_project_report.html) | `af7d718` (the session branch `praneetdhoolia/rule-on-the-five-blocking-defects`, four commits past the PR #152 merge) | 7 September 2026 | **22 defects, 53 risks and 47 smells at HEAD, the first pass to measure the synthetic demand against the package's own observations**: the synthetic day puts 19.6 % of departures after 20:00 against 6.2 % observed and 6.4 % after midnight against none, a third of the taxi excess is night trips the model manufactures, the public-transport day is inverted against the Hunter's Opal taps (school peak 31.6 % observed against 8.5 % modelled), income and occupation are drawn independently of age while income scales the marginal utility of money, car-less households are 36 % lone-person against 65 % nationally, own-LGA commuting runs 7–26 points under the 2011 journey-to-work table and none of the 12.8 % out-of-region commuters exist; of the morning report's 95 findings 24 are closed on the four new commits and 46 still open; **the first F30 arm is running** (launched 15:08, 375 s an iteration beside a stray report-session python burning a core since 6 Sep) and the board is stale against it; top optimisation: Parse once into a link table, apply the four stamps in memory, write once through det_io (touches nothing); the factor ledger moves five statuses IN → PARTIAL on the measured demand (now IN 35 · PARTIAL 28 · INERT 1 · ASC 2 · OUT 6) and its top mover is population synthesis; the field library adds MWCOG Gen3, the UCLA Deep Activity Model and the Brussels cellphone MATSim (44 projects, 12 platforms) and the per-mode-ridership rung is still empty with this project 1 of 12 modes inside 10 % |
| [`20260907T130247_project_report.html`](20260907T130247_project_report.html) | `419b0da` (the PR #146 merge, on `main`) | 7 September 2026 | 34 defects, 37 risks and 24 smells at HEAD, led by a **measured holdout breach** — the heavy-vehicle share converting 31 of 34 count targets is a median over 23 stations of which 20 are holdout, and the artefact prints the contradiction itself — and by a hardcoding scanner that reads only module-level ALL-CAPS names, so a CI gate is green on a rule that is broken; 7 of the 7 Sep report's 87 findings closed, 80 still open on a six-file diff; the iteration was fixed rather than the model — our listeners fell from 56.2 % of an iteration to 0.8 %, the median iteration from 639 s to 258.5 s, and a 250-iteration arm now fits in 18–19 h against 44 h, so **cost is no longer the constraint and convergence has still never been observed** (highest `reached_iteration` across 153 runs is 100); top optimisation: let trim reclaim the two pre-§9.142 arms (336 GiB of a 87 %-full cache, touches nothing); still the only project of 41 aiming at the per-mode-ridership rung and still not on it — 1 of 12 modes inside 10 % (car +6.6 %, the first ever at a gate), 7 past the stop bar; top missing factor: the inert crowding utility against heavy rail at +295.1 %; the reference library's two dead lanes are closed — **0 → 12 platform rows, 0 → 58 factor literature halves** |
| `20260907T013735_project_report.html` (pruned in `03b5220`) | `bdd40f3` (the session branch, five commits past the PR #144 merge) | 7 September 2026 | 33 defects, 34 risks and 8 smells at HEAD, four defects in the model itself (the engines' BeforeMobsim plan writes discarded, coherence proposals teleported, SCATS grants never undone, the ten-iteration reader blind under the new cadence); all 14 of the 3 Sep defects closed; top optimisation: route the fallback walk inside the engines (20-60 s an iteration, opens a family); alone on the top validation rung by design and no mode inside 10 % by result; top missing factor: an inert crowding utility against heavy rail at +234.7 %; the platform and factor research lanes were cut short by the search budget |
| `20260903T134517_project_report.html` (pruned in `03b5220`) | `9c99e54` (the PR #110 merge) | 3 September 2026 | 14 defects confirmed at HEAD; no unit tests; the gate watcher would kill a passing arm; two acquisition scripts cannot reproduce their outputs; 471 of 509 manifest rows carry no licence; findings filed as #112–#137 |
