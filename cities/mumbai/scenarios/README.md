# Scenarios

One scenario, `BASE`: the base year as acquired, declared in `../overlays/scenarios/BASE.json`.
The assembled run inputs (`scenarios/matsim/`) are not built for this city yet: the bounded baseline case
prepares its network and schedule at launch (`src/run/baseline_smoke.py`, decision 9.202, #238).
