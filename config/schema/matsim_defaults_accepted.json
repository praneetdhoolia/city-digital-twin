{
  "$comment": "PORTABLE. Every parameter the pinned framework decides for itself in a module this model writes into, with the reason its default is accepted. It carries no model value: a value the model CHOOSES is declared in a city registry, not accepted here. An entry is a claim - 'the framework's choice here is right, or irrelevant, for this model' - and it is reviewable because it had to be written down. A parameter that decides something about the transport system MUST NOT be added here: declare it. Read by src/registry/check_matsim_defaults.py; the surface it is checked against is config/schema/matsim_defaults.json.",
  "matsim_version": "2027.0-2026w25",
  "opened": "2026-09-07",
  "why": "DECISIONS.md 9.154. RUN.travel_time.analysed_modes was declared ['car'], swept, rendered into the reference and proven to reach the config - and meant nothing, because travelTimeCalculator.filterModes was never emitted and its default of false tells MATSim to ignore analyzedModes (#154). Every check here asked whether a DECLARED value reaches the model; none could ask what an UNDECLARED one was deciding beside it. This ledger is that question, asked once per parameter.",
  "accepted": {
    "eventsManager.estimatedNumberOfEvents": "a sizing hint for the events pipeline's buffers. It changes how much memory the manager pre-allocates, never which events are produced or in what order.",
    "eventsManager.eventsQueueSize": "the depth of the queue between the qsim and the handler threads. A wall-time and memory knob; every handler still receives the complete stream in per-handler order (9.56).",
    "global.defaultDelimiter": "the delimiter MATSim writes its own tabular outputs with. A file format, not a model value.",
    "global.insistingOnDeprecatedConfigVersion": "false refuses a config written to a superseded schema. Accepting the refusal is the conservative choice and the emitter writes the current version anyway.",
    "global.relativeToleranceForSampleSizeFactors": "0.0 is the STRICTEST setting: MATSim throws when qsim.flowCapacityFactor and qsim.storageCapacityFactor differ at all. RUN.sample.storage_capacity_exponent is 1.0 precisely so they are equal (its own field says so), and a looser tolerance would let them drift silently.",
    "qsim.delayInstantTeleportationArrivals": "whether a teleported arrival is processed at the next time step rather than instantly - a one-timestep shift in when an arrival EVENT fires, not a change to any travel time or any choice. It touches this model only through the pt access and egress legs that are still teleported (#167); when those become network legs it is inert entirely.",
    "qsim.filterSnapshots": "which vehicles a snapshot file would show. RUN.qsim.snapshot_period is 0, so no snapshot is ever written.",
    "qsim.linkWidth": "the width a link is DRAWN at in a visualiser. No snapshots are written.",
    "qsim.nodeOffset": "how far a drawn link is offset from its node in a visualiser. No snapshots are written.",
    "qsim.notifyAboutStuckVehicles": "whether the mobsim logs an extra notification for a stuck vehicle. A diagnostic channel, and not the one this project reads: stuck agents are counted from PersonStuckEvent in the run's own events stream, which is emitted either way.",
    "qsim.personInitializedEvents": "which additional events, if any, are emitted when a person is placed at the start of the mobsim. An event-stream option: it adds records to the log, and no agent behaves differently for it.",
    "qsim.snapshotStyle": "the shape a visualiser would draw vehicles in. No snapshots are written.",
    "qsim.timeStepSize": "the mobsim's clock, 1 s. It is MATSim's own step and the step every published MATSim result is computed on; changing it would change the physics of every queue in the model, and there is no reason from this city to move it. Recorded here so that a future change is a decision rather than a discovery.",
    "qsim.useLanes": "false. Lanes are not modelled and are refused LOUDLY where they would matter: GradientSignalsNetworkFactory throws if gradient.representation is link_speed and lanes are on, because the lanes delegate is not ported (9.84).",
    "qsim.usePersonIdForMissingVehicleId": "the identifier a vehicle receives when a leg names none. An id convention. Every mode in this model carries a declared vehicle type through vehicles.vehiclesFile, so the fallback decides the SPELLING of an id and never a physical property.",
    "replanning.ExternalExeConfigTemplate": "configuration for MATSim's external-executable replanning module, which this model does not install.",
    "replanning.ExternalExeTimeOut": "the same external-executable module.",
    "replanning.ExternalExeTmpFileRootDir": "the same external-executable module.",
    "routing.networkRouteConsistencyCheck": "what happens when a plan's route does not match the network it is run on. The default ABORTS, which is the behaviour this project wants everywhere: an inconsistent route silently simulated is exactly the class of defect the run-record contract exists to stop. Accepting it is accepting a refusal, not choosing a value.",
    "scoring.usingOldScoringBelowZeroUtilityDuration": "a compatibility switch that selects a RETIRED activity-scoring formula for durations below the zero-utility point. The default keeps the current formula, which is the one C.scoring.* is expressed against and the one the MATSim reference text documents. Switching it would score this model against a formula no declared value was derived for.",
    "scoring.writeExperiencedPlans": "whether the executed plans are written each iteration. An OUTPUT, and one this project depends on: src/analyse/iteration_trips.py derives a twelve-mode reading at every plans-writing iteration from exactly this file (9.120). The default is on and turning it off would remove a measurement, not change a model.",
    "scoring.writeScoreExplanations": "whether MATSim writes a per-agent prose explanation of its score. Output only, and large.",
    "transit.inputScheduleCRS": "null means the schedule is already in the run's coordinate system, which it is: the schedule is built by pt2matsim against the same EPSG:28356 network the run loads.",
    "transit.insistingOnUsingDeprecatedAttributeFiles": "false refuses a superseded attribute-file format. The schedules this model builds do not use one.",
    "transit.routingAlgorithmType": "which transit routing algorithm is used. The default is SwissRailRaptor and this model is BUILT on it rather than having inherited it: citysim.RaptorModeCostCalculator extends its in-vehicle cost calculator and C.raptor.mode_cost_representation gates that extension (9.160, 9.162). It is recorded here so the dependency is visible; the choice itself is in the record.",
    "transit.usingTransitInMobsim": "whether transit vehicles are physically simulated or their passengers teleported. GOAL.md requirement 1 admits no teleportation, so the default is the only value consistent with the goal, and the alternative is not a modelling option this project has.",
    "travelTimeCalculator.calculateLinkToLinkTravelTimes": "false, and it must be: controler.linkToLinkRouting is false, and MATSim throws outright if link-to-link travel times are calculated together with separateModes.",
    "travelTimeCalculator.calculateLinkTravelTimes": "true - the observed link travel times ARE the feedback this model routes on (issue #28). Accepting the default is accepting the mechanism the study depends on.",
    "travelTimeCalculator.maxTime": "108000 s = 30 h, which equals RUN.qsim.end_time_h x 3600 exactly. It is the horizon past which the calculator stops binning; a shorter one would silently drop the late tail of the simulated day. IT MUST TRACK RUN.qsim.end_time_h: if that field ever moves past 30 h, this stops being an accepted default and becomes a declaration."
  }
}
