{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/praneetdhoolia/city-digital-twin/config/schema/outputs/run.schema.json",
  "title": "_run.json - the run record",
  "description": "Written by src/run/run_matsim.py when a run ends at a DEFINED BOUNDARY: it ran every declared iteration, or it was deliberately stopped - by the gate watcher under the GOAL.md loop, or by the operator through run.py --stop. A run that CRASHED leaves none, and that is still what separates a run whose reading can be cited from a directory that cannot explain itself. The record no longer means 'this run converged': `completion` says which boundary ended it, and only `ran_to_last_iteration` means the run executed the horizon it declared. Readers that mean 'a complete arm' MUST ask `completion`, not mere presence - resume matching and the calibrated base both do. Records written before that field existed were only ever written on rc=0, so a MISSING `completion` reads as `ran_to_last_iteration`.",
  "type": "object",
  "required": [
    "name",
    "scenario",
    "day",
    "fraction",
    "iterations",
    "threads",
    "seed",
    "rc",
    "wall_s",
    "completion"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Run directory name; the run identity."
    },
    "scenario": {
      "type": "string",
      "description": "Validated against the CITY'S OWN declared intervention.scenarios at write time (src/registry/outputs.py injects the enum from city.json; issue #62 A1) - the portable schema file itself names no city's vocabulary."
    },
    "day": {
      "type": "string",
      "description": "Validated against the CITY'S OWN declared day_types at write time (src/registry/outputs.py injects the enum from city.json; issue #62 A1) - the portable schema file itself names no city's vocabulary."
    },
    "fraction": {
      "type": "number",
      "exclusiveMinimum": 0,
      "maximum": 1.0,
      "description": "Sample fraction. Every extensive quantity below is scaled by it."
    },
    "iterations": {
      "type": "integer",
      "minimum": 0
    },
    "threads": {
      "type": "integer",
      "minimum": 1,
      "description": "PART OF THE RUN IDENTITY, not a performance knob: MATSim partitions the network by thread count, so two runs that differ only here are not the same run."
    },
    "xmx": {
      "type": "string",
      "description": "JVM heap, e.g. '14g'."
    },
    "seed": {
      "type": "integer"
    },
    "overrides": {
      "type": "object",
      "description": "Config overrides applied to this run."
    },
    "config_snapshot": {
      "type": "string",
      "description": "Relative path to the resolved registry snapshot for this run. A result without one cannot state what inputs produced it."
    },
    "rc": {
      "type": [
        "integer",
        "null"
      ],
      "description": "Process return code. 0 on a run that reached its horizon; the JVM's non-zero code on a gate stop. NULL on `stopped_by_operator`: --stop kills the harness as well as the JVM, so no process survived to observe a code, and recording a made-up one would state something the run did not."
    },
    "wall_s": {
      "type": "number",
      "minimum": 0
    },
    "median_iteration_s": {
      "type": [
        "number",
        "null"
      ],
      "minimum": 0
    },
    "persons_in": {
      "type": [
        "integer",
        "null"
      ],
      "minimum": 0
    },
    "persons_kept": {
      "type": [
        "integer",
        "null"
      ],
      "minimum": 0
    },
    "transit_capacity_scaled": {
      "type": "array",
      "description": "Per vehicle type: [attribute, before, after]. Empty at fraction 1.0. Seats MUST scale with the sample or a 10% run gives every bus ten times its real capacity and crowding silently disappears."
    },
    "controler_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "sha256 over the committed source of src/java/citysim, the MATSim entry point this run executed. The run name is built from the scenario and the registry values and cannot see the controler, so without this a result produced before a controler change and one produced after would share a name and be indistinguishable. run_matsim.py refuses to resume a completed run whose recorded hash differs from the current source. Absent from records written before DECISIONS.md 9.26."
    },
    "inputs_sha256": {
      "type": [
        "string",
        "null"
      ],
      "description": "sha256 of the day's population file the run sampled from (DECISIONS.md 9.127): part of the run's identity, so a rebuilt demand under the same parameters is a different run and cannot resume an earlier one; null when the file was absent."
    },
    "warm_started_from": {
      "type": "object",
      "description": "Present only on a warm-started run (issue #75): the dead parent run and the plans checkpoint iteration this run continued from. A warm-started run is NOT bit-identical to an uninterrupted one (DECISIONS.md 9.76), so this key is part of run identity and resume matching.",
      "required": [
        "run",
        "iteration"
      ],
      "properties": {
        "run": {
          "type": "string"
        },
        "iteration": {
          "type": "integer",
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "completion": {
      "type": "string",
      "enum": [
        "ran_to_last_iteration",
        "stopped_at_gate",
        "stopped_by_operator"
      ],
      "description": "Which boundary ended the run. `ran_to_last_iteration` - the run executed every iteration it declared (this is NOT a claim of convergence; DECISIONS.md 9.7 holds that separately). `stopped_at_gate` - the runner's own watcher stopped it at a GOAL.md gate because a mode was at or past the stop bar. `stopped_by_operator` - a person or session stopped it through run.py --stop with a stated cause. A stopped run's reading up to `reached_iteration` is real and citable; the run is NOT a complete arm and must never anchor a calibrated base or satisfy resume."
    },
    "reached_iteration": {
      "type": [
        "integer",
        "null"
      ],
      "minimum": 0,
      "description": "The last iteration the run actually ended, read from its own progress digest or log. Equals `iterations` on `ran_to_last_iteration`. On a stopped run this is the iteration the reading belongs to, and citing any figure from the run without it states more than the run did."
    },
    "stop_cause": {
      "type": [
        "string",
        "null"
      ],
      "description": "Why a stopped run stopped, quoted from the gate watcher's verdict or the operator's stated cause - never composed. Null on `ran_to_last_iteration`."
    }
  },
  "additionalProperties": true
}
